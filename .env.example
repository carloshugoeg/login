PORT=3000
BCRYPT_ROUNDS=12

# sqlite   = base embebida en un archivo local (no requiere servidor)
# postgres = requiere un servidor PostgreSQL y DATABASE_URL
DB_ENGINE=sqlite

# ---- Solo para DB_ENGINE=sqlite ----
DATABASE_PATH=./data/app.db

# ---- Solo para DB_ENGINE=postgres ----
DATABASE_URL=postgres://usuario:password@localhost:5432/registro_lab
# Base aparte para las pruebas de integración (npm test la trunca):
TEST_DATABASE_URL=postgres://usuario:password@localhost:5432/registro_lab_test

# Con esta URL se arma el enlace del correo. Si envías a otra persona,
# localhost NO le servirá: necesitas una URL pública (ver README).
APP_BASE_URL=http://localhost:3000

# console = imprime el correo en la terminal
# smtp    = lo envía de verdad
MAIL_TRANSPORT=console

# ---- Solo para MAIL_TRANSPORT=smtp ----
# Gmail: SMTP_PASSWORD es una CONTRASEÑA DE APLICACIÓN de 16 letras
# (myaccount.google.com/apppasswords), nunca la de tu cuenta.
# MAIL_FROM suele tener que coincidir con SMTP_USER.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu-correo@gmail.com
SMTP_PASSWORD=
MAIL_FROM="Registro Lab <tu-correo@gmail.com>"
